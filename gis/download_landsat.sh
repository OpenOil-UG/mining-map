#! /bin/bash

# load the OSM sites and compute the centerpoints, write them to a file



# for each of the points, ask the API for the most recent scene with the
# least cloud cover and write the scene names to a file



# loop over the file and remove duplicate scenes (we dont want to
# download them multiple times)



# download each of the scenes and process them (pensharpening)


# cut them 
